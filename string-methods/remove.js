let str = +prompt(`enter number`)
let replaceWith = prompt(`enter replacing element`)

// console.log(str.replaceAll(Number , replaceWith))
for (let i = 0; i <str.length; i++) {
    if(str.charAt(i)==Number) {
        str.replace(Numbers, "a")
    }

}
// console.log(str)
// we can do it by find the first index and the last index with indexOf and lastindexOf and use slice method to show it



